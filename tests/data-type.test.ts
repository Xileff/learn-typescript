describe('Data Type', (): void => {
  it('must declare correctly', (): void => {
    const name: string = 'Felix';
    const balance: number = 10000000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
